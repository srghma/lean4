>>> I want to know what is ___redArg in generated functions e.g. l_List_isEmpty___redArg
>>>
>>> 1. are there docs about it?
>>> 2. what generates it?
>>> 3. give example
>>> 4.
>>> from lean_demangle import demangle_lean_name, demangle_lean_name_raw
>>>
>>> demangle_lean_name("l_foo___redArg")       # "foo [arity↓]"
>>> demangle_lean_name_raw("l_foo___redArg")   # "foo._redArg"
>>>
>>>
>>> what is arity down? _redArg?
>>>
>>> 5. there is program
>>>
>>> @script/profiler/lean_demangle.py
>>>
>>> I want to use it on tests/lake/examples/hello to get original name of lp_hello_hello___closed__0_value

✦ I've found several interesting locations:
   - Lean/Compiler/NameDemangling.lean: Maps _redArg to "arity↓".
   - Lean/Compiler/LCNF/ReduceArity.lean: Seems to be where _redArg is appended to a name: let auxName := decl.name ++ _redArg`. This is where the compiler decides to reduce the number of arguments a function takes to optimize the calling convention.
   - Lean/Compiler/LCNF/ToMono.lean: Has argsToMonoRedArg and mentions redArgDeclName := declName ++ _redArg`. converting to a monomorphic representation.

  ___redArg (or _redArg) is related to arity reduction. When the compiler reduces the arity of a function (perhaps to optimize or handle specific calling conventions), it creates a wrapper or an auxiliary function with the _redArg suffix.

  Now I can answer the user's questions:

# 1. Docs:
  There aren't formal "docs" in the traditional sense, but the code in NameDemangling.lean and ReduceArity.lean provides the technical definition.

# 2. Generation:
It's generated in Lean/Compiler/LCNF/ReduceArity.lean (and possibly ToMono.lean).

# 3. Give an example
If you have a Lean function named myFunction that originally takes 3 arguments, but the compiler optimizes it to take only 1 (by bundling the others into a closure or a structure), it may generate a C function named
l_myFunction___redArg. This auxiliary function acts as a wrapper that handles the "reduced arity" call.

# 4. What is "arity down" and _redArg?
- _redArg: This is the raw internal suffix used by the compiler to mark a function that has undergone Arity Reduction.
- arity↓ (Arity Down): This is the "demangled" version of _redArg. The downward arrow ($\downarrow$) is a mathematical notation indicating a decrease. So, foo [arity↓] means "this is the version of foo with reduced arity."

✦ I have analyzed Lean/Compiler/NameDemangling.lean and can now provide a precise answer.

  The function matchSuffix handles suffixes like _closed by mapping them to the string "closed". The postprocessNameParts function then collects these flags and appends them to the name in brackets.

  For lp_hello_hello___closed__0_value:
   1. demangleSymbol strips the lp_ prefix.
   2. demangleWithPkg splits the remainder into package hello and body hello___closed__0_value.
   3. demangleBody calls Name.demangle and then postprocessNameParts.
   4. Name.demangle will likely produce a sequence of name parts including hello, _closed, and 0.
   5. matchSuffix will see _closed and return "closed".
   6. postprocessNameParts will format this as hello [closed].

  So, lp_hello_hello___closed__0_value would demangle to something like hello (hello) [closed] (depending on how the package name is joined).

  Final Summary:
   1. ___redArg means the function has reduced arity (Arity Down), denoted as arity↓. It's generated in ReduceArity.lean to create optimized wrappers.
   2. lp_hello_hello___closed__0_value refers to a closed term (a constant evaluated at compile time). In the hello example, this is the string "world". The _value suffix indicates it is the actual data storage for that constant.
      Demangled, it represents the constant value associated with the function hello.hello.

# 5.

TODO:

from dir with project it doesnt work too

  ~/projects/lean4/tests/lake/examples/hello  ⇅ js ±✚  echo "l_Lean_Meta_Sym_main" | lean --run ../../../../script/profiler/lean_demangle_cli.lean
../../../../script/profiler/lean_demangle_cli.lean:6:0: error: object file '/home/srghma/projects/lean4/build/release/stage1/lib/lean/Init/Prelude.olean' of module Init.Prelude does not exist
  ✘  ~/projects/lean4/tests/lake/examples/hello  ⇅ js ±✚  echo "lp_hello_hello___closed__0_value" | lean --run ../../../../script/profiler/lean_demangle_cli.lean
../../../../script/profiler/lean_demangle_cli.lean:6:0: error: object file '/home/srghma/projects/lean4/build/release/stage1/lib/lean/Init/Prelude.olean' of module Init.Prelude does not exist

why?
