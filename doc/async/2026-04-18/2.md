file1:
into docs add md file, it should contain graph of all files in Lean/Compiler

and tell what each of modules is used for.

----
file2:

is this paths correct?
  1. Standard Path (The Heavy-Duty Optimizer) Source -> Expr -> Decl -> Code (LCNF) -> IR -> LLVM IR -> Machine Code
  2. Current C++ Path (The Runtime-Based Path) Source -> Expr -> Decl -> Code (LCNF) -> C++ Source


write for each path to what datatypes code is transformed, why for, add examples


----
file3:

I want to generate EmitEs6 from lean code
which path You propose?

Suggested JS Path (Your target) Source -> Expr -> Decl -> Code (LCNF) -> JS Source?

or maybe better to gen js not from Code, but from Decl?
or maybe better to gen js not from Decl, but from Expr?

how each path will look like (input code, output js)

pros and cons

-----

file4:

write plan what I need to learn to implement EmitEs6 and plan how to acheive this

-----

add another file with answer to:

I thought to port https://github.com/aristanetworks/purescript-backend-optimizer on lean (make it gen purescript, but all in lean) and see which datatypes can be used

is it better to transform Expr to WHICH datatype in aristanetworks/purescript-backend-optimizer? or Decl? or Code?
