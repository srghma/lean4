## 1. Rust and affine / linear behavior

Same for Rust?

It is using affine types, right? Which is same like linear, just doesnt know when object is removed. But used-exclusively/shared is exposed inside of type system. Right?

So how it happens. It is a USER who should write two variants of same function (`lean_array_push` and `lean_array_push__reusing`)? (make example if yes)

And if 2 arguments - then 4 different variants of same function? (make example if yes)

---

## 2. About `append(a, b)`

You wrote definition is

```c
if (lean_is_exclusive(a)) {
    // mutate a to include b
} else {
    // allocate new array c, copy a into c, copy b into c
}
```

but it could be improved to

```c
if (lean_is_exclusive(a)) {
    // mutate a to include b
} else if (lean_is_exclusive(b)) {
    // mutate b to include a
} else {
    // allocate new array c, copy a into c, copy b into c
}
```

therefore here 3 variants of same function. Or can improve even more?

I will probably have to do the same in javascript (implement 3 variants of same function). How it would look like then?

---

## 3. Compile-time optimization in C++

Will c++ compiler optimize `array_push` at compile time if all data is known at compile time?

I mean compiler should be able to optimize both

```lean
x := ([].push 1).push 2
```

and

```lean
init := [].push 1
x := init.push 2
y := init.push 3
```

if code flow is not dependent on runtime value. So does it optimize?
