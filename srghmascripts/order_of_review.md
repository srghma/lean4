# C++ to Rust Porting Review Order

Files are topologically sorted by dependency order (leaves first).
Siblings at the same indentation level are independent and can be reviewed in any order.

- [x] include/lean/lean (`include/lean/lean.h`)
  - [x] include/lean/lean_gmp (`include/lean/lean_gmp.h`)
  - [x] runtime/stack_overflow (`runtime/stack_overflow.h` and `runtime/stack_overflow.cpp`)
  - [x] library/llvm (`library/llvm.cpp`)
  - [x] runtime/alloc (`runtime/alloc.h` and `runtime/alloc.cpp`)
  - [x] runtime/byteslice (`runtime/byteslice.h` and `runtime/byteslice.cpp`)
  - [x] runtime/debug (`runtime/debug.h` and `runtime/debug.cpp`)
    - [x] runtime/hash (`runtime/hash.h` and `runtime/hash.cpp`)
    - [x] runtime/init_module (`runtime/init_module.h` and `runtime/init_module.cpp`)
    - [x] runtime/mpn (`runtime/mpn.h` and `runtime/mpn.cpp`)
      - [x] runtime/mpz (`runtime/mpz.h` and `runtime/mpz.cpp`)
        - [x] runtime/object (`runtime/object.h` and `runtime/object.cpp`)
          - [x] runtime/object_ref (`runtime/object_ref.h` and `runtime/object_ref.cpp`)
            - [x] runtime/option_ref (`runtime/option_ref.h`)
            - [x] runtime/pair_ref (`runtime/pair_ref.h`)
              - [x] runtime/process (`runtime/process.h` and `runtime/process.cpp`)
              - [x] util/kvmap (`util/kvmap.h` and `util/kvmap.cpp`)
            - [x] runtime/sharecommon (`runtime/sharecommon.h` and `runtime/sharecommon.cpp`)
            - [x] runtime/string_ref (`runtime/string_ref.h`)
              - [x] util/ffi (`util/ffi.cpp`)
              - [x] util/io (`util/io.h`)
            - [x] util/nat (`util/nat.h`)
          - [x] runtime/platform (`runtime/platform.h` and `runtime/platform.cpp`)
          - [x] runtime/uv/event_loop (`runtime/uv/event_loop.h` and `runtime/uv/event_loop.cpp`)
            - [x] runtime/uv/signal (`runtime/uv/signal.h` and `runtime/uv/signal.cpp`)
            - [x] runtime/uv/system (`runtime/uv/system.h` and `runtime/uv/system.cpp`)
            - [x] runtime/uv/timer (`runtime/uv/timer.h` and `runtime/uv/timer.cpp`)
          - [x] runtime/uv/net_addr (`runtime/uv/net_addr.h` and `runtime/uv/net_addr.cpp`)
            - [x] runtime/uv/tcp (`runtime/uv/tcp.h` and `runtime/uv/tcp.cpp`)
            - [x] runtime/uv/udp (`runtime/uv/udp.h` and `runtime/uv/udp.cpp`)
          - [x] util/init_module (`util/init_module.h` and `util/init_module.cpp`)
          - [x] util/map_foreach (`util/map_foreach.h` and `util/map_foreach.cpp`)
    - [x] runtime/optional (`runtime/optional.h`)
      - [x] runtime/utf8 (`runtime/utf8.h` and `runtime/utf8.cpp`)
        - [x] util/name (`util/name.h` and `util/name.cpp`)
          - [x] util/name_generator (`util/name_generator.h` and `util/name_generator.cpp`)
          - [x] util/name_hash_map (`util/name_hash_map.h`)
          - [x] util/name_hash_set (`util/name_hash_set.h`)
          - [x] util/name_map (`util/name_map.h`)
            - [x] util/option_declarations (`util/option_declarations.h` and `util/option_declarations.cpp`)
              - [x] util/options (`util/options.h` and `util/options.cpp`)
          - [x] util/name_set (`util/name_set.h` and `util/name_set.cpp`)
      - [x] util/list (`util/list.h`)
        - [x] util/list_fn (`util/list_fn.h` and `util/list_fn.cpp`)
      - [x] util/path (`util/path.h` and `util/path.cpp`)
        - [x] util/shell (`util/shell.cpp`)
      - [x] util/rb_tree (`util/rb_tree.h`)
    - [x] util/bit_tricks (`util/bit_tricks.h` and `util/bit_tricks.cpp`)
  - [x] runtime/mutex (`runtime/mutex.h` and `runtime/mutex.cpp`)
  - [x] runtime/uv/dns (`runtime/uv/dns.h` and `runtime/uv/dns.cpp`)
- [x] include/lean/lean_libuv (`include/lean/lean_libuv.h`)
- [x] library/suffixes (`library/suffixes.h`)
- [x] runtime/flet (`runtime/flet.h`)
  - [x] kernel/equiv_manager (`kernel/equiv_manager.h` and `kernel/equiv_manager.cpp`)
- [x] runtime/int (`runtime/int.h`)
  - [x] util/message_definitions (`util/message_definitions.h`)
- [x] runtime/sstream (`runtime/sstream.h`)
  - [x] library/dynlib (`library/dynlib.h` and `library/dynlib.cpp`)
  - [x] runtime/exception (`runtime/exception.h` and `runtime/exception.cpp`)
    - [x] runtime/interrupt (`runtime/interrupt.h` and `runtime/interrupt.cpp`)
      - [x] runtime/thread (`runtime/thread.h` and `runtime/thread.cpp`)
        - [x] util/rc (`util/rc.h`)
        - [x] util/timer (`util/timer.h` and `util/timer.cpp`)
    - [x] runtime/memory (`runtime/memory.h` and `runtime/memory.cpp`)
      - [x] runtime/stackinfo (`runtime/stackinfo.h` and `runtime/stackinfo.cpp`)
        - [x] util/test (`util/test.h`)
    - [x] util/exception_with_pos (`util/exception_with_pos.h`)
- [x] shell/lean (`shell/lean.cpp`)
- [x] shell/lean_js (`shell/lean_js.cpp`)
- [x] util/alloc (`util/alloc.h`)
  - [x] library/scope_cache (`library/scope_cache.h`)
  - [x] runtime/compact (`runtime/compact.h` and `runtime/compact.cpp`)
- [x] util/ascii (`util/ascii.h` and `util/ascii.cpp`)
- [x] util/escaped (`util/escaped.h` and `util/escaped.cpp`)
- [x] util/freset (`util/freset.h`)
- [x] util/lbool (`util/lbool.h` and `util/lbool.cpp`)
- [x] util/macros (`util/macros.h`)
- [x] util/null_ostream (`util/null_ostream.h`)
  - [x] util/output_channel (`util/output_channel.h`)
- [x] util/pair (`util/pair.h`)
  - [x] util/rb_map (`util/rb_map.h`)
- [x] util/timeit (`util/timeit.h` and `util/timeit.cpp`)
- [x] util/unit (`util/unit.h`)
- [x] util/unlock_guard (`util/unlock_guard.h`)
- [x] initialize/init (`initialize/init.h` and `initialize/init.cpp`)
- [x] kernel/abstract (`kernel/abstract.h` and `kernel/abstract.cpp`)
  - [x] kernel/expr (`kernel/expr.h` and `kernel/expr.cpp`)
    - [x] kernel/expr_cache (`kernel/expr_cache.h` and `kernel/expr_cache.cpp`)
    - [x] kernel/expr_eq_fn (`kernel/expr_eq_fn.h` and `kernel/expr_eq_fn.cpp`)
    - [x] kernel/expr_maps (`kernel/expr_maps.h`)
      - [x] kernel/replace_fn (`kernel/replace_fn.h` and `kernel/replace_fn.cpp`)
        - [x] kernel/type_checker (`kernel/type_checker.h` and `kernel/type_checker.cpp`)
          - [x] library/elab_environment (`library/elab_environment.h` and `library/elab_environment.cpp`)
            - [x] library/init_attribute (`library/init_attribute.h` and `library/init_attribute.cpp`)
              - [x] library/ir_interpreter (`library/ir_interpreter.h` and `library/ir_interpreter.cpp`)
            - [x] library/ir_types (`library/ir_types.h`)
            - [x] library/module (`library/module.h` and `library/module.cpp`)
    - [x] kernel/expr_sets (`kernel/expr_sets.h`)
      - [x] kernel/for_each_fn (`kernel/for_each_fn.h` and `kernel/for_each_fn.cpp`)
    - [x] kernel/find_fn (`kernel/find_fn.h`)
      - [x] kernel/inductive (`kernel/inductive.h` and `kernel/inductive.cpp`)
        - [x] kernel/init_module (`kernel/init_module.h` and `kernel/init_module.cpp`)
    - [x] kernel/instantiate (`kernel/instantiate.h` and `kernel/instantiate.cpp`)
      - [x] library/instantiate_mvars (`library/instantiate_mvars.cpp`)
      - [x] library/replace_visitor (`library/replace_visitor.h` and `library/replace_visitor.cpp`)
        - [x] library/util (`library/util.h` and `library/util.cpp`)
    - [x] kernel/local_ctx (`kernel/local_ctx.h` and `kernel/local_ctx.cpp`)
      - [x] kernel/quot (`kernel/quot.h` and `kernel/quot.cpp`)
      - [x] kernel/trace (`kernel/trace.h` and `kernel/trace.cpp`)
      - [x] library/expr_lt (`library/expr_lt.h` and `library/expr_lt.cpp`)
        - [x] library/expr_pair (`library/expr_pair.h`)
          - [x] library/expr_pair_maps (`library/expr_pair_maps.h`)
    - [x] library/annotation (`library/annotation.h` and `library/annotation.cpp`)
    - [x] library/bin_app (`library/bin_app.h` and `library/bin_app.cpp`)
    - [x] library/expr_unsigned_map (`library/expr_unsigned_map.h`)
    - [x] library/formatter (`library/formatter.h` and `library/formatter.cpp`)
      - [x] library/init_module (`library/init_module.h` and `library/init_module.cpp`)
      - [x] library/print (`library/print.h` and `library/print.cpp`)
    - [x] library/max_sharing (`library/max_sharing.h` and `library/max_sharing.cpp`)
- [x] kernel/declaration (`kernel/declaration.h` and `kernel/declaration.cpp`)
  - [x] kernel/environment (`kernel/environment.h` and `kernel/environment.cpp`)
    - [x] kernel/kernel_exception (`kernel/kernel_exception.h`)
    - [x] kernel/level (`kernel/level.h` and `kernel/level.cpp`)
- [x] library/constants (`library/constants.h` and `library/constants.cpp`)
  - [x] library/constructions/cases_on (`library/constructions/cases_on.h` and `library/constructions/cases_on.cpp`)
  - [x] library/constructions/util (`library/constructions/util.h` and `library/constructions/util.cpp`)
  - [x] library/num (`library/num.h` and `library/num.cpp`)
- [x] library/constructions/init_module (`library/constructions/init_module.h` and `library/constructions/init_module.cpp`)
- [x] library/constructions/no_confusion (`library/constructions/no_confusion.h`)
- [x] library/profiling (`library/profiling.h` and `library/profiling.cpp`)
  - [x] library/time_task (`library/time_task.h` and `library/time_task.cpp`)
- [x] runtime/allocprof (`runtime/allocprof.h` and `runtime/allocprof.cpp`)
  - [x] runtime/io (`runtime/io.h` and `runtime/io.cpp`)
  - [x] runtime/libuv (`runtime/libuv.h` and `runtime/libuv.cpp`)
- [x] runtime/apply (`runtime/apply.h` and `runtime/apply.cpp`)
- [x] runtime/array_ref (`runtime/array_ref.h`)
- [x] runtime/buffer (`runtime/buffer.h`)
  - [x] runtime/list_ref (`runtime/list_ref.h`)
