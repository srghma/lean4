[package]
name = "lean_runtime_common"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libc = "0.2"
libloading = "0.9"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(lean_lazy_rc)'] }
